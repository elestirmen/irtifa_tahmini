# input_images

Bu klasör ham (augment edilmemiş) giriş görüntülerini içindir.

- Varsayılan olarak `run_pipeline.ps1` `input_images/` yolunu kullanır.
- Görüntü dosyaları (örn. `.jpg/.jpeg`) GitHub'a gönderilmez; repoda sadece kod ve `.md` dosyaları tutulur.
